namespace VeradeAddin.Logging
{
    /// <summary>Result of a command execution, recorded on every log entry.</summary>
    public enum LogOutcome
    {
        Success,
        Cancel,
        Error
    }
}
